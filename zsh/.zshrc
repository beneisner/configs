# This is the base ZSH config file for me. Very straightforward, just loads in some default ZSH settings
# from my config repository.
CONFIG_DIR=${HOME}/code/configs

source ${CONFIG_DIR}/zsh/base.zsh
# source ${CONFIG_DIR}/zsh/fast_paste.zsh